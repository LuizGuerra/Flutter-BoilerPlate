name: desktop_boilerplate
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # Input verification
  fzregex: ^2.0.0
  # Storage dependencies
  localstorage: ^4.0.0+1
  connectivity_plus: ^2.3.0
  # Image managers
  cached_network_image: ^3.2.1
  # Color dependencies
  dynamic_color: ^1.3.0
  material_color_utilities: ^0.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/images/
    # - assets/icons/